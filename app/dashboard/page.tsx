import {
    Flex,
} from '@chakra-ui/react'

function Profile() {
    return (
        <Flex position={'relative'} top={0} left={0}>
            Dashboard
        </Flex>
    )
}

export default Profile;